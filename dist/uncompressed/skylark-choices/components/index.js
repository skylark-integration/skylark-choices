define([
    './dropdown',
    './container',
    './input',
    './list',
    './wrapped-input',
    './wrapped-select'
], function (Dropdown, Container, Input, List, WrappedInput, WrappedSelect) {
    'use strict';
    return {
        Dropdown,
        Container,
        Input,
        List,
        WrappedInput,
        WrappedSelect
    };
});